// Expected rejection: IO exposes no cancellable arbitrary-blocking operation.
import IO

func unavailable(
    borrowing descriptor: borrowing Kernel.Descriptor
) async throws(Event.Operation.Error<Never>) {
    _ = try await IO.blocking.run(borrowing: descriptor) { _ in () }
}
